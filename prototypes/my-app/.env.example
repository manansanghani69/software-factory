# Database — SQLite local file (Drizzle)
DATABASE_URL=file:./db.sqlite

# Uncomment and configure when adding auth via Auth.js
# AUTH_SECRET=generate-with-openssl-rand-base64-32
# GITHUB_ID=
# GITHUB_SECRET=
